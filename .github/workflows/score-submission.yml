name: Score Submission

on:
  pull_request:
    paths:
      - 'submissions/*.csv'

jobs:
  score:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repository
      - uses: actions/checkout@v3

      # 2. Set up Python
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      # 3. Install dependencies
      - name: Install dependencies
        run: pip install -r starter_code/requirements.txt

      # 4. Dynamically extract submitted CSV (CORRECT WAY)
      - name: Get submission file
        id: get_submission
        run: |
          BASE_SHA=${{ github.event.pull_request.base.sha }}

          FILES=$(git diff --name-only $BASE_SHA...HEAD | grep '^submissions/.*\.csv')

          if [ -z "$FILES" ]; then
            echo "Error: No submission CSV found"
            exit 1
          fi

          if [ $(echo "$FILES" | wc -l) -ne 1 ]; then
            echo "Error: Exactly one CSV submission is required"
            exit 1
          fi

          echo "su
